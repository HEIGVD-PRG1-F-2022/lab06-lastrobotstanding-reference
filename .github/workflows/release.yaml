name: Releasing Objects

on: [push]

jobs:
  compile:
    name: Compile .o files and release them
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Create temporary folder
        run: mkdir zip

      - name: Configure CMake
        run: cmake ${{github.workspace}}

      - name: Build .obj and zip
        run: |
          cmake --build .
          echo "COUCOU"
          Get-ChildItem -Path ./ReferenceGame.dir/Debug
          Compress-Archive -LiteralPath ReferenceGame.dir/Debug/ -DestinationPath zip/ReferenceGame.zip

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.tag }}
          draft: false
          prerelease: false

      - name: Upload zip as release asset
        id: upload-release-asset
        uses: actions/upload-release-asset@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./zip/ReferenceGame.zip
          asset_content_type: application/zip